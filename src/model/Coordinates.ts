export class Coordinates {
  /** number of pixels from left of image */
  x: number;
  /** number of pixels from top of image */
  y: number;

  constructor(x: number, y: number) {
    this.x = x;
    this.y = y;
  }

}
