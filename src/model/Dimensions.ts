export class Dimensions {
  width!: number;
  height!: number;

  /**
   * @param {number} w - width
   * @param {number} h - height
   */
  constructor(w: number, h: number) {
    this.width = w;
    this.height = h;
  }

}
